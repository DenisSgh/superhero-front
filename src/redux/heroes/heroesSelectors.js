export const getHeroes = state => state.heroes.items;
export const getHero = state => state.heroes.item;
export const getTotalDocs = state => state.heroes.totalDocs;
export const getPage = state => state.heroes.page;
